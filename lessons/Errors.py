# syntax error - when the code structure or syntax is wrong
# name error - when a variable is not defined
# index error - when you give an index that is not in the range
# module not found error - when importing a module that doesn’t exist or is not installed
# attribute error - when you call an attribute or method that the object/module doesn’t have
# key error - when calling a non-existent or wrong key in a dictionary
# type error - when operating on data types that aren’t compatible
# import error - when importing a function or variable that doesn’t exist in the module
# value error - when the right data type is used but with an invalid value
